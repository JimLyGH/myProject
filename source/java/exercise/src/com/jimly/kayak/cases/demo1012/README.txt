接口 对象 = new 实现类();		//方式1
与
实现类 对象 = new 实现类();	//方式2

使用方式1的好处：接口的设计就是为了实现多态,便于程序的二次开发与维护

例如:demo1012案例中,假设useDatabase方法中的形参使用实现类而不用接口,
那么,此时我想再增加一个SqlServer的数据库该怎么办?
新增一个useDatabase方法并且形参为Sqlserver.

而如果用接口作为形参呢?
解决策略,无需新增方法,直接在需要的时候把SqlServer的实现类传进去就OK啦!